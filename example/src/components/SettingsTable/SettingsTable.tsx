import { SxProps, Table } from "@mui/material"


export interface SettingsTableProps {

}

const TableStyle: SxProps = {
	background: ""
}

export default function SettingsTable (props: SettingsTableProps) {
	return (
		<Table sx={ TableStyle }>

		</Table>
	)
}